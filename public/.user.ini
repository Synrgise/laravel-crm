; Allow larger uploads for SLA & contract documents (app validates max 10 MB).
; Used by PHP-FPM when allowed by server. For Apache mod_php, see .htaccess instead.
upload_max_filesize = 20M
post_max_size = 20M
